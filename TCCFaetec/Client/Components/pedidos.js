/*function selecionarItem(element) {

    // Remove o background branco dos outros itens

    const items = document.querySelectorAll('#navPedidos li');
    items.forEach(item => item.style.backgroundColor = '');
  
    // Adiciona o background branco ao item clicado

    element.style.backgroundColor = 'white';
  }
  const modal = document.getElementById("VizualizarPedidos");

// Função para abrir a modal

function abrirModalVizu() {
  modal.style.display = "block";
}

function closeModal() {
  modal.style.display = "none";
}

// Fecha a modal ao clicar fora dela
window.onclick = function(event) {
  const modal = document.getElementById("VizualizarPedidos");
  if (event.target == modal) {
    modal.style.display = "none";
  }
}*/